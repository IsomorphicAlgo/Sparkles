# Overlay: day-trade v2 + G1 + G2 (merge onto rklb_daytrade_v2.yaml)
# Prerequisite: sparkles label -c configs/experiments/rklb_daytrade_v2.yaml

train:
  experiment_name: rklb_daytrade_g1_g2_v1
  notes: "Day-trade v2 + G1 intraday + G2 session/volume/VWAP"
  export_predictions: val

features:
  returns_multi_horizon: true
  returns_horizons_bars: [3, 5, 10, 15, 30]
  realized_vol_multi: true
  realized_vol_windows_bars: [5, 15, 30]
  realized_vol_include_ratio: true
  range_vol_multi: true
  range_vol_window_bars: 15
  range_vol_include_atr_norm: true
  session_time: true
  volume_context: true
  volume_median_window_bars: 60
  vwap_distance: true
